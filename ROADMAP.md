# Дорожная карта

Цель: Build Codex Autopilot v0.9.0-beta on the actual v0.8 implementation: add a dependency-aware deterministic AI Studio Runtime with safe parallel fresh specialist workers, verified dependency gates, independent and deterministic verification, resource coordination, concurrent Project Memory, canonical project placement, deterministic human-readable thread naming, recovery, compatibility, documentation, live acceptance evidence, and local release artifacts without pushing, tagging, publishing, weakening security, or discarding user changes.

- [x] M1: Specify DAG contracts and additive migration — code — рассуждение: max
  - Act as the architecture and schema engineer. Re-audit v0.8, then define and implement the v0.9 structured task graph, dynamic role profiles, verification policies, resource claims, unambiguous task state machine, cycle validation, and additive loading/migration of v0.8 serial plans. Preserve Project Memory rather than creating a second subsystem.
  - Причина режима: Repository inspection, Python implementation, schemas, documentation, and deterministic tests are sufficient.
  - Критерий готовности: The canonical v0.9 plan/task schema covers every required and optional field from the goal, validates references and cycles on initial load and plan changes, and represents a legacy serial plan as a valid DAG.
  - Критерий готовности: Task-state transitions are explicit and mechanically validated, including the invariant IMPLEMENTED != VERIFIED and dependency eligibility based on required verification.
  - Критерий готовности: Existing v0.8 plan/config/state fixtures load or migrate safely without silently enabling parallel execution.
  - Критерий готовности: Focused deterministic tests for schema validation, cycle detection, transition legality, and migration pass, and architecture decisions are documented with evidence.
- [x] M2: Implement deterministic dependency scheduler — code — рассуждение: xhigh
  - Act as the scheduler engineer. Implement the non-LLM dependency-aware scheduler over the new graph and durable state, including READY computation, verified prerequisite gates, bounded concurrency, deterministic priority ordering, capability limits, and serial/parallel/auto strategies.
  - Причина режима: The scheduler is local deterministic Python and can be verified exhaustively with unit tests.
  - Критерий готовности: Scheduler decisions are ordinary deterministic code with no model call and use a documented stable priority policy covering explicit priority, critical-path or fan-out relevance, age, and resource availability.
  - Критерий готовности: The reference DAG A+B, C<-A, D<-A+B, E<-C+D produces the exact expected readiness and verified-unlock sequence.
  - Критерий готовности: max_parallel_workers has a conservative documented default and execution_strategy=serial preserves one-at-a-time compatibility.
  - Критерий готовности: Focused scheduler, dependency-gate, fairness, and serial-compatibility tests pass.
- [x] M3: Add resource claims and durable locks — code — рассуждение: xhigh
  - Act as the concurrency-safety engineer. Implement normalized resource claims, read/write/exclusive conflict detection, ownership metadata, computer_use slots, acquisition/release journaling, stale-lock reconciliation, and a simple reliable shared-working-tree strategy that remains compatible with git.auto_commit=false.
  - Причина режима: Resource coordination and crash behavior can be implemented and verified through local state-machine tests without GUI interaction.
  - Критерий готовности: read/read is allowed; read/write and write/write conflict; exclusive blocks matching resources; path, directory, glob, application, environment, browser, device, external sandbox, and logical resources have documented matching semantics.
  - Критерий готовности: computer_use_slots defaults conservatively to 1 and does not block unrelated code workers.
  - Критерий готовности: A crashed lock owner cannot cause permanent deadlock or an unsafe duplicate writer; reconciliation is fail-closed until authoritative worker state is known.
  - Критерий готовности: Resource conflict, non-mergeable resource, lock persistence, stale recovery, and Computer Use serialization tests pass.
- [x] M4: Run parallel App Server workers safely — code — рассуждение: max
  - Act as the App Server/runtime engineer. Refactor the one-worker dispatcher into a durable multi-worker lifecycle that creates independent fresh threads, tracks authoritative turn completion, keeps bounded concurrent sessions, safely handles partial failures and rate limits, and never introduces a persistent controller LLM.
  - Причина режима: App Server protocol changes and concurrent lifecycle behavior are testable with repository code and deterministic fakes.
  - Критерий готовности: Multiple independent READY code tasks can have overlapping active App Server turns up to max_parallel_workers while conflicting or dependent tasks remain waiting.
  - Критерий готовности: Every external create/start/wait/interrupt operation is journaled with task/thread/turn identity so ambiguous recovery cannot duplicate work or unlock dependencies.
  - Критерий готовности: One worker rate limit or failure preserves graph state and permits other safe work while deterministic retry behavior remains intact.
  - Критерий готовности: Mock App Server tests prove two parallel Sol workers, authoritative completion, dependency blocking, bounded concurrency, crash recovery, and no unlimited thread creation.
- [x] M5: Build verification and revision lifecycle — code — рассуждение: max
  - Act as the verification-system engineer. Implement self, deterministic, independent, and auto verification policies; fresh verifier threads and selective prompts; PASS/REVISE parsing; revision attempts; verifier capability routing; and dependency unlock only after the policy reaches VERIFIED.
  - Причина режима: Verification contracts, prompt construction, routing, and revision state transitions can be implemented and tested programmatically.
  - Критерий готовности: An implementer completion produces IMPLEMENTED, not VERIFIED, when further verification is required, and downstream tasks stay blocked until verification succeeds.
  - Критерий готовности: Deterministic exhaustive evidence is a fail-fast precheck and acceptance input; every policy reaches VERIFIED only through a fresh independent verifier that does not receive implementer self-assessment or unnecessary transcript history.
  - Критерий готовности: REVISE creates a fresh revision worker with structured issues, increments revision numbers deterministically, and sends the result to a fresh verifier again.
  - Критерий готовности: Tests cover implementer false success -> REVISE -> revision -> VERIFIED, verifier independence, deterministic verification, routing, and dependency unlock.
- [x] M6: Add dynamic roles and AI Studio context — code — рассуждение: high
  - Act as the worker-context engineer. Implement arbitrary temporary specialist role profiles, planner-assigned task metadata, bounded selective Project Memory and dependency-output context, and the AI Studio Runtime abstraction without making role determine model or retaining worker sessions.
  - Причина режима: Role metadata, context selection, prompts, and synthetic structures are file- and test-verifiable.
  - Критерий готовности: Arbitrary role names and profiles define responsibilities, domain focus, preferred tools, context priorities, and verification expectations while model remains capability-routed.
  - Критерий готовности: Implementation, verification, revision, planning, and replanning prompts contain only their structured task, role, DoD, relevant verified state, dependency outputs, issues, and resources; they exclude full transcripts and concurrent conversations.
  - Критерий готовности: Synthetic code/integration, research/fact-verification, and mixed code/Computer Use structures can be represented by the same role/task runtime.
  - Критерий готовности: Context-boundary, role-routing, cost-control, and fresh-thread tests pass.
- [x] M7: Guarantee project placement titles and status UX — code — рассуждение: xhigh
  - Act as the Codex workspace integration engineer. Make target_project_root canonical for every run-created planner, implementer, verifier, revision worker, and replanner; preserve supported saved-project association; generate deterministic concise human-readable thread titles from structured metadata; and expose semantic project status.
  - Причина режима: App Server request/response contracts, title metadata, cwd, projectId, and status output can be exercised through code and protocol fakes.
  - Критерий готовности: Every thread/start uses canonical target_project_root as cwd even when the initiating thread is elsewhere, and a matching explicit or longest-root saved projectId is used and verified when App Server supports it.
  - Критерий готовности: Implementation T44, verifier T44, revision T44-R1, initial planner, and PC replanner title functions produce the exact required human-readable formats without UUIDs or redundant project prefixes.
  - Критерий готовности: The dispatcher calls thread/name/set, reads actual App Server metadata where available, and fails or reports exact limitations instead of claiming unsupported UI association.
  - Критерий готовности: Status reports Running, Verifying, Waiting with reasons, Ready, verified progress, worker slots, Computer Use slots, and exact active titles; focused placement/title/status tests pass.
- [x] M8: Implement plan evolution pause and recovery — code — рассуждение: max
  - Act as the resilience engineer. Add structured PLAN_CHANGE_REQUEST handling through short fresh planner/replanner turns, graph versioning and revalidation, multi-worker pause/resume semantics, rate-limit coordination, and crash-safe reconciliation of running work and locks.
  - Причина режима: Plan-change and recovery semantics are local journaled state-machine behavior with deterministic tests.
  - Критерий готовности: A worker can request a structured prerequisite/dependency/resource/verification change; a fresh planner reads current verified state, atomically updates an acyclic graph, and exits without becoming a manager.
  - Критерий готовности: Pause stops new launches, handles already-running workers according to documented deterministic semantics, persists graph and locks, and Resume safely reconciles before creating fresh continuation work.
  - Критерий готовности: Rate limits and process crashes preserve task attempts, verification state, dependencies, ownership, and retry timing without duplicate advancement.
  - Критерий готовности: Dynamic prerequisite, cycle rejection, multi-worker pause/resume, rate-limit, and crash/lock recovery tests pass.
- [x] M9: Harden concurrent Project Memory — code — рассуждение: xhigh
  - Act as the Project Memory concurrency engineer. Reuse and extend the v0.8 SQLite/MCP subsystem so simultaneous workers can safely record evidence, observations, decisions, verification results, and conflicts with transactional IDs, crash safety, bounded retrieval, and no summary-of-summary drift.
  - Причина режима: SQLite transaction behavior and concurrent MCP writes can be stress-tested locally without GUI interaction.
  - Критерий готовности: Concurrent writes use explicit safe transactions and bounded busy handling with no duplicate IDs, lost updates, broken references, or database corruption.
  - Критерий готовности: Verification outcomes and task/thread provenance integrate with existing evidence rules; independent agreement alone never creates Truth and NO EVIDENCE -> NO TRUTH remains enforced.
  - Критерий готовности: Rendering/backups/recovery cannot race writers into inconsistent or partial generated state.
  - Критерий готовности: A repeatable concurrent stress suite covers evidence, observations, decisions, verification results, conflicts, integrity_check, ID uniqueness, and recovery, and it passes.
- [ ] M10: Repair branch audit — code — рассуждение: max
  - Act as a fresh independent release verifier on branch repair/m10-p0. The branch carries commits that CLAIM to close items REV-002 through REV-006 of your own earlier audit in docs/RELEASE_VERIFICATION_0.9.0-beta.md, plus machine-checkable enforcement for rules R1, R5, R6, R7, R13, R16, R17, R18 and R21, plus normalized incident signatures with two-level recovery. Treat every one of those as a claim, not as a result: verify each against actual source and a named test. Commit messages, prior worker statements and report prose are not evidence. Do not re-audit closed items from zero and do not revert repaired code. Report what is genuinely closed, what is still open, and what cannot be assessed without a live App Server.
  - Причина режима: Independent repository review, tests, diffs, documentation, and security scans are sufficient for this verification milestone.
  - Критерий готовности: Every claimed closure is confirmed or refuted against actual source and a named test; commit messages and report prose are never accepted as evidence.
  - Критерий готовности: The complete deterministic suite passes in a clean environment with no CODEX_THREAD_ID, CODEX_TURN_ID or CODEX_SESSION_ID set.
  - Критерий готовности: Items that require a live App Server, including REV-007 and REV-008, stay marked NOT TESTED with the exact reason instead of being declared closed.
  - Критерий готовности: The audit report gains a dated section for this pass listing the remaining actionable items for M11, and earlier findings are not silently dropped.
  - Критерий готовности: No repaired code is reverted, no rule enforcement is weakened, and no safety bypass, automatic approval, unrestricted filesystem or shell MCP, silent permission escalation, or automatic push/tag/release is introduced.
- [ ] M11: Run live acceptance and prepare beta artifacts — computer_use — рассуждение: max
  - Act as the live acceptance and release engineer. Resolve every actionable item from the independent audit, then use the actual installed/supported Codex App Server and real Computer Use only where safely available to validate parallel workers, Computer Use serialization, independent verification/revision, dependency unlock, pause/recovery, canonical project placement, and actual thread titles. Finish documentation, versioning, reproducible source/release ZIPs, test report, checksums, and an exact final report; do not push, tag, or publish. The repair branch repair/m10-p0 is the starting state: its rule enforcement and Pipeline Engineer work is already in place, so resolve only the items M10 leaves open rather than rebuilding them.
  - Причина режима: The final acceptance explicitly requires a real Astra Computer Use scenario plus actual desktop/runtime behavior that deterministic code alone cannot fully establish.
  - Критерий готовности: All deterministic tests and validators pass after revisions, the source tree contains version 0.9.0-beta documentation and migration guidance, and release artifacts are reproducibly built and contamination-scanned.
  - Критерий готовности: Safe real tests cover two parallel Sol workers and supported Sol+Astra/Computer Use behavior, including computer_use_slots=1, or each unexecutable case is explicitly marked NOT TESTED with the exact product/runtime limitation and no fabricated result.
  - Критерий готовности: Actual App Server/Desktop metadata is checked for implementation, verifier, revision, planner/replanner cwd, projectId association, and deterministic titles where the current API exposes it; unsupported UI guarantees are reported exactly.
  - Критерий готовности: The final verification report contains the requested 57-item completion summary, performance/usage measurements available from evidence, exact upgrade/install command, exact first-use prompt, absolute artifact paths, and SHA-256 values.
