__pycache__/
*.py[cod]
.DS_Store
.venv/
dist/
build/
*.zip
.codex-autopilot/


# Autopilot generates a roadmap in every project it runs in. In this
# repository it is the leftover of the run that built the product.
ROADMAP.md

# Working notes that are not part of the product.
docs/V1_TARGET.md
docs/V1_RUN.md

# The catalogue of applied runtime patches: the state of a machine that
# repaired itself, not source.
patches/
