__pycache__/
*.py[cod]
.DS_Store
.venv/
dist/
build/
*.zip
.codex-autopilot/


# Дорожная карта генерируется автопайлотом в каждом проекте.
# В репозитории самого скилла это остаток прогона, которым его строили.
ROADMAP.md

# Внутренние документы линии v1.0: целевая спецификация и инициирующий
# запрос прогона. Рабочий план и коммерческое позиционирование, а не
# документация продукта. Лежат в рабочем дереве, но не в публичном
# репозитории; из пользовательских архивов их исключает INTERNAL_DOCS.
docs/V1_TARGET.md
docs/V1_RUN.md

# Каталог применённых правок рантайма: состояние машины, а не исходник.
patches/
